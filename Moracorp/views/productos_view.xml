<odoo>
  <data>
    <!-- actions opening views on models -->
    
    <record model="ir.actions.act_window" id="productos_action_window">
      <field name="name">Productos de Moracorp Electrics</field>
      <!-- Nombre del modelo que mostraremos en la vista-->
      <field name="res_model">moracorp.productos</field>
      <!-- Indicamos que nuestro formulario tendrá vista tree (para mostrar todos los datos
      y vista form (para crear o editar datos)
      La vista Form no la definimos, por lo cual tendremos la version automatica.
      La vista tree la definiremos mas abajo.
      -->
      <field name="view_mode">tree,form</field>
    </record>


    <!-- Top menu item 
    En este ejemplo, todo el menu de "menuitem" esta puesto como ejemplo, no hace una función concreta
    -->
    <menuitem name="Moracorp Electronics" id="moracorp_menu_root" />

    <!-- actions -->
    <menuitem name="Productos" id="productos_menu" parent="moracorp_menu_root" action="productos_action_window" />


    <!-- Odoo puede colocar vistas automaticas, aunque con un aspecto y funcionalidad mejorable
    Nosotros, para afinarlo, explicaremos de manera explicita como es cada vista -->

    <!-- Definimos como es la vista explicita de la lista-->

    <!-- Vista Tree -->
    <record model="ir.ui.view" id="productos_tree_view">
      <field name="name">Lista de Productos</field>
      <field name="model">moracorp.productos</field>
      <field name="arch" type="xml">
        <tree>
          <field name="id" />
          <field name="n_serie" />
          <field name="nombre" />
          <field name="marca" />
          <field name="foto" widget="image" class= "oe_avatar"/>
          <field name="descripcion" />
          <field name="precio" />
          <field name="porcentaje_iva" />
          <field name="precio_iva"/>
        </tree>
      </field>
    </record>

    <!-- Vista Form -->
    <record model="ir.ui.view" id="productos_form_view">
        <field name="name">Formulario de Productos</field>
        <field name="model">moracorp.productos</field>
        <field name="arch" type="xml">
            <form>
                <group>
                    <group>
                        <field name="n_serie" />
                    </group>
                    <group>
                        <field name="nombre" />
                    </group>
                    <group>
                        <field name="marca" />
                    </group>
                    <group>
                        <field name="foto" widget="image" class="oe_avatar" />
                    </group>
                    <group>
                        <field name="descripcion" />
                    </group>
                    <group>
                        <field name="precio" />
                    </group>
                    <group>
                        <field name="porcentaje_iva" />
                    </group>
                </group>
            </form>
        </field>
    </record>
  </data>
</odoo>